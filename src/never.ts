// const message: string = "Some error";

// const error = (msg: string): never => {
//   throw new Error(msg);
// };

// error(message);

// const infiniteLoop = (): never => {
//   while (true) {}
// };
